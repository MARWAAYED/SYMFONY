{% extends 'base.html.twig' %}

{% block title %} Modification de mot de passe {% endblock %}

{% block body %}
    <div class="container">
        <h1>Modification du mot de passe</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
            <button class="btn btn-success mt-3">Modifier le mot de passe</button>
        {{ form_end(form) }}
    </div>
{% endblock %}